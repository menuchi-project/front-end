import { PersianPricePipe } from './persian-price.pipe';

describe('PersianPricePipe', () => {
  it('create an instance', () => {
    const pipe = new PersianPricePipe();
    expect(pipe).toBeTruthy();
  });
});
